{
  "openapi": "3.0.0",
  "info": {
    "title": "Simulator Télos - Movie API",
    "description": "API para gerenciamento de catálogo de filmes e usuários - Nível #10 (Segurança JWT)",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://localhost:3000",
      "description": "Servidor Local"
    }
  ],
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "Movie": {
        "type": "object",
        "properties": {
          "_id": { "type": "string", "example": "663a7f8e1c2b3a4d5e6f7a8b" },
          "title": { "type": "string", "example": "Inception" },
          "description": { "type": "string" },
          "year": { "type": "integer" },
          "genres": { "type": "array", "items": { "type": "string" } },
          "image": { "type": "string" },
          "video": { "type": "string" }
        }
      },
      "MovieInput": {
        "type": "object",
        "required": ["title", "description", "year", "genres", "image", "video"],
        "properties": {
          "title": { "type": "string" },
          "description": { "type": "string" },
          "year": { "type": "integer" },
          "genres": { "type": "array", "items": { "type": "string" } },
          "image": { "type": "string" },
          "video": { "type": "string" }
        }
      },
      "User": {
        "type": "object",
        "properties": {
          "_id": { "type": "string" },
          "name": { "type": "string" },
          "email": { "type": "string" }
        }
      },
      "UserInput": {
        "type": "object",
        "required": ["name", "email", "password"],
        "properties": {
          "name": { "type": "string" },
          "email": { "type": "string" },
          "password": { "type": "string" }
        }
      }
    }
  },
  "paths": {
    "/auth/login": {
      "post": {
        "tags": ["Auth"],
        "summary": "Realizar login e obter Token JWT",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["email", "password"],
                "properties": {
                  "email": { "type": "string", "example": "hugo2@email.com" },
                  "password": { "type": "string", "example": "senhaSegura2123" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Login realizado com sucesso" },
          "401": { "description": "Credenciais inválidas" }
        }
      }
    },
    "/movies": {
      "get": {
        "summary": "Listar todos os filmes (Público)",
        "tags": ["Movies"],
        "responses": {
          "200": { "description": "Sucesso" }
        }
      },
      "post": {
        "summary": "Criar novo filme (Protegido)",
        "security": [{ "bearerAuth": [] }],
        "tags": ["Movies"],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/MovieInput" }
            }
          }
        },
        "responses": {
          "201": { "description": "Criado com sucesso" },
          "401": { "description": "Token não fornecido ou inválido" }
        }
      }
    },
    "/movies/{id}": {
      "get": {
        "summary": "Buscar filme por ID",
        "tags": ["Movies"],
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "Sucesso" } }
      },
      "put": {
        "summary": "Atualizar filme (Protegido)",
        "security": [{ "bearerAuth": [] }],
        "tags": ["Movies"],
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/MovieInput" } } }
        },
        "responses": { "200": { "description": "Atualizado" } }
      },
      "delete": {
        "summary": "Deletar filme (Protegido)",
        "security": [{ "bearerAuth": [] }],
        "tags": ["Movies"],
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "204": { "description": "Deletado" } }
      }
    },
    "/users": {
      "get": {
        "summary": "Listar usuários (Protegido)",
        "security": [{ "bearerAuth": [] }],
        "tags": ["Users"],
        "responses": { "200": { "description": "Sucesso" } }
      },
      "post": {
        "summary": "Criar novo usuário",
        "tags": ["Users"],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/UserInput" } } }
        },
        "responses": { "201": { "description": "Criado" } }
      }
    }
  }
}